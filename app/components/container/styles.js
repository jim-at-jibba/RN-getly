import { create } from 'react-native-platform-stylesheet';

export default create({
  wrapper: {
    display: 'flex',
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },

  listWrapper: {
    display: 'flex',
    flex: 1,
  },

  formWrapper: {
    display: 'flex',
    flex: 1,
    margin: 20
  }
});
